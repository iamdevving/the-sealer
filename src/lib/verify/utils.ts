// src/lib/verify/utils.ts
// Shared helpers for the verification layer.
//
// computeProofPoints() was removed — replaced by computeScoring() in scoring.ts.
// Import from scoring.ts for all achievement score / leaderboard points computation.